const initialState = [
	{
		name: '',
		age: 200,
		height: '',
		id: 0,
	},
];

export const smurfReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
